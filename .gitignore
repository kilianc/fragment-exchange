# Build output. bin/vercel is a checked-in script, not a build artefact.
bin/server

.DS_Store
*.log
.vercel/

# Raw notes: mine, not the repository's.
reflextion.md
*.notes.md

# Vercel CLI credentials, written by bin/vercel. Never commit these.
.vercel-auth/
